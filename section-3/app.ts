let myName: string = 'Scotty';
let myAge: number = 31;
let anything: any;
anything = 12;

// myName = 30;

function controlMe(isTrue: boolean, somethingElse: boolean) {
  let result: number;
  if (isTrue) {
    result = 12;
  }
  result == 33;
  return result;
}